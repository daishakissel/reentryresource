# Scripts

## export-db-schema

Exports the current database schema and enumeration values to `/docs/DATABASE_SCHEMA_CURRENT.md`.

```bash
npm run export-db-schema
```

Requires `NEXT_PUBLIC_SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` in `.env.local`.
